{% extends 'base.html.twig' %}

{% block block2 %}
{% autoescape false %}
<table class="table table">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Menu</th>
        <th scope="col">Date</th>
        <th scope="col">Actions</th>
      </tr>
    </thead>
    <tbody>
        <tr>
    {% if newsletter is empty %}
    <td>Aucune newsletter</td> 
    {% endif %}
    </tr>
    </tbody>
{% for x in newsletter %}
        {# <p> {{ x.menu.date|date("d/m/Y") }} </p>
        <p> {{ x.titre }} </p> #}

  <tbody>
    <tr>
      <td scope="row">{{ x.id }}</td>
      <td> {{ x.titre }}</td>
      <td> {{ x.menu.date|date("d/m/Y") }}</td>
      
      
      <td>
        <a href="/sendnewsletter/{{ x.id }}" class="btn btn-primary btn-success">Envoyer</a>
        <a href="/archivenewsletter/{{ x.id }}" class="btn btn-primary btn-warning">Archiver</a>
      </td>
      
    </tr>
  </tbody>


{% endfor %}
</table>
{% endautoescape %}


{% endblock %}